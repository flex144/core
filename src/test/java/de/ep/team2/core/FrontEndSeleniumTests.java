package de.ep.team2.core;

import org.junit.Test;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.firefox.FirefoxDriver;

public class FrontEndSeleniumTests {

    /* disabled cause it takes too long
    @Test
    public void exampleTestYoutube() {
        //System.setProperty("webdriver.gecko.driver", //URL);

        WebDriver driver = new FirefoxDriver();

        driver.navigate().to("https://www.youtube.com/");

        String currentTitle = driver.getTitle();

        if(currentTitle.contentEquals("YouTube")) {
            System.out.println("Test for Youtube passed!");
        } else {
            System.out.println("Test for Youtube failed");
        }

        driver.quit();
    }
    */
}
